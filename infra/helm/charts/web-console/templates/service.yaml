apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-console.fullname" . }}
  labels:
    {{- include "web-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "web-console.selectorLabels" . | nindent 4 }}
