"""dataspace-control-plane-adapters — anti-corruption layer.

Implements every core/ port for external protocols, products, and runtimes.
Import only from subpackage api.py surfaces; never from internal modules directly.
"""

__version__ = "0.1.0"
