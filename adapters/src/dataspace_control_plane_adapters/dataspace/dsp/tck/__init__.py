"""DSP Technology Compatibility Kit (TCK) integration stubs.

Run the full DSP TCK suite with::

    pytest tests/compatibility/dsp-tck

This package is intentionally minimal — it contains only the hooks and
fixtures needed to wire the DSP adapter into the external TCK harness.
"""
from __future__ import annotations
