"""DCP TCK (Test Compatibility Kit) package stub.

Run the DCP compatibility suite with:

    pytest tests/compatibility/dcp-tck

The TCK tests validate that this adapter implementation is conformant with
the Dataspace Credential Protocol 1.0 specification. Tests live in
``tests/compatibility/dcp-tck/`` and import the DCP adapter clients from
this package for black-box protocol verification.
"""
from __future__ import annotations
