"""
org_packs/ — In-repo organization-specific pack implementations.

Drop your actual org-specific packs here. Each pack gets its own subdirectory
following the same template structure as custom/templates/.

This directory is gitignored by default in public forks. Add your org-specific
pack subdirectory to .gitignore or to a private overlay repo as appropriate.
"""
